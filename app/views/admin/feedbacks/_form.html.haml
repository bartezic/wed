= simple_form_for([:admin, @feedback]) do |f|
  = f.error_notification

  .form-inputs
    = f.association :partner
    = f.input :subject
    = f.input :name
    = f.input :email
    = f.input :msg

  .form-actions
    = f.button :submit
