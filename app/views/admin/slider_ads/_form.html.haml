= simple_form_for([:admin, @slider_ad]) do |f|
  = f.error_notification
  .row
    .col-xs-6
      = f.association :partner,     input_html: { class: 'form-control' }
      = f.input :asset,             input_html: { class: 'form-control' }
      = f.input :asset_remote_url,  input_html: { class: 'form-control' }
      = f.input :active
      = f.input :active_to
      = f.simple_fields_for :text do |d|
        = d.input :title,   input_html: { class: 'form-control', value: f.object.text && f.object.text['title'] }
        = d.input :subtitle,input_html: { class: 'form-control', value: f.object.text && f.object.text['subtitle'] }
        
      = f.button :submit, class: 'btn btn-default'