%h1 Listing videos

.table-responsive
  %table.table.table-bordered.table-striped
    %thead
      %tr
        %th Name
        %th Description
        %th Cover
        %th cover_remote_url
        %th Rating
        %th Link
        %th Partner
        %th Actions
    %tbody
      - @videos.each do |video|
        %tr
          %td= video.name
          %td= video.description
          %td= image_tag video.cover(:thumb_150)
          %td= video.cover_remote_url
          %td= video.rating
          %td= video.link
          %td
            - if video.partner
              = link_to video.partner.name, video.partner
          %td
            = link_to 'Show', [:admin, video]
            = link_to 'Edit', edit_admin_video_path(video)
            = link_to 'Destroy', [:admin, video], :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Video', new_admin_video_path
