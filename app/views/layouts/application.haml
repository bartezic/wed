!!!
%html
  %head
    %title Wed
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body

    / Wrap all page content here
    #wrap
      / Fixed navbar
      .navbar.navbar-default.navbar-fixed-top
        .container.top-nav
          .navbar-header
            %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
              %span.icon-bar
              %span.icon-bar
              %span.icon-bar
          .navbar-collapse.collapse
            %ul.nav.navbar-nav

            %ul.nav.navbar-nav.navbar-right
              %li.locale{class: locale == :uk && 'active', data: { locale: :uk }}= link_to "Укр", "?locale=uk"
              %li.locale{class: locale == :ru && 'active', data: { locale: :ru }}= link_to "Рус", "?locale=ru"
            /.nav-collapse
        .container
          .navbar-header
            %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
              %span.icon-bar
              %span.icon-bar
              %span.icon-bar
            %a.navbar-brand{href: "/"} Logo
          .navbar-collapse.collapse
            %ul.nav.navbar-nav

            %ul.nav.navbar-nav.navbar-right
              - if user_signed_in?
                %li.dropdown
                  %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
                    = current_user.email 
                    %b.caret
                  %ul.dropdown-menu
                    %li
                      %a{href: "/admin"} Адмінка
                    %li= link_to 'Вийти', destroy_user_session_path, { method: :delete }
              - elsif user_signed_in?
                %li.dropdown
                  %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
                    Ви ввійшли як
                    = current_user.name
                    %b.caret
                  %ul.dropdown-menu
                    %li= link_to 'Кабінет', [:cabinet, current_partner]
                    %li= link_to 'Вийти', destroy_user_session_path, { method: :delete }
              - else
                %button.btn.btn-default.navbar-btn{ type: :button, data:{ toggle: :modal, target: :'#modal-login'} } Ввійти
                %button.btn.btn-default.navbar-btn{ type: :button } Стати партнером
            /.nav-collapse
      / Begin page content
      .container
        .row
          .col-md-3
            %ul.nav.nav-pills.nav-stacked
              - Category.includes(:translations).each do |category|
                %li= link_to raw("#{category.name} <span class='badge pull-right'>#{category.partners.count}</span>"), category
          .col-md-9
            = yield
    #footer
      .container
        %p.text-muted.credit
          Розробка:
          %a{href: "http://martinbean.co.uk"} Олексин Віктор

    = render 'shared/popups'