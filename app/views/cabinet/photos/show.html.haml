%p#notice= notice

%dl
  %dt Asset
  %dd= image_tag @photo.asset(:original), class: 'img-responsive'

  %dt Rating
  %dd= @photo.rating

  %dt Gallery
  %dd= link_to(@photo.gallery.name, [:cabinet, @photo.gallery]) if @photo.gallery

= link_to 'Edit', edit_cabinet_photo_path(@photo), class: 'btn btn-default'
= link_to 'Back', cabinet_photos_path, class: 'btn btn-default'
