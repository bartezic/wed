.edit-form
  = simple_form_for [:cabinet, gallery], remote: true do |f|
    = f.error_notification
    
    = f.input :name,        input_html: { class: 'form-control' }, label: 'Назва галереї'
    = f.input :description, input_html: { class: 'form-control' }, label: 'Опис галереї', as: :text
    
    = f.button :submit, class: 'btn btn-default'